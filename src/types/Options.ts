interface Options {
  backgroundColor: string;
  borderColor: string;
  borderSize: string;
  columns: number;
  pixelOffColor: string;
  pixelOnColor: string;
  pixelSize: number;
  rows: number;
}

export default Options;

export type { Options };
